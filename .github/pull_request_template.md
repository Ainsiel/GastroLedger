## Work Order

- Work order:
- Feature branch:
- Target branch:

## Evidence

- [ ] Acceptance criteria are mapped to tests.
- [ ] `npm run lint` passes.
- [ ] `npm test` passes.
- [ ] `npm run build` passes.
- [ ] No secret values or external integrations were introduced.
- [ ] Scope remains inside one approved work order.
- [ ] Frontend-bearing changes follow the approved UI Spec and shadcn/ui system.
- [ ] Frontend-bearing changes include accessible behavior, integrated route and
      390/1024/1440 visual QA evidence.

## Delivery

- Feature PRs target `develop`.
- Release PRs originate from `develop` and target `main`.
- Merge, deploy and remote configuration changes require separate approval.
